#pragma once

#include <string.h>

class User {
  public: 
    
      // Switching between modes inside the text editor
      void selectMode(char& command);
      void inputMode(char& command);
      void controlMode(std::string& command);
      
      
                           


}; 